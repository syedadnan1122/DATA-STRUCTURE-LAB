<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arena of Ratings - Advanced BST System</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #000;
            color: #fff;
            overflow-x: hidden;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 60px 40px;
            position: relative;
        }

        /* Animated Background */
        .bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            background: radial-gradient(ellipse at top, #1a1a2e 0%, #0a0e27 50%, #000 100%);
        }

        .particle {
            position: absolute;
            width: 2px;
            height: 2px;
            background: #00f3ff;
            border-radius: 50%;
            opacity: 0.6;
            animation: float-particles 20s infinite;
        }

        @keyframes float-particles {
            0%, 100% { transform: translate(0, 0); opacity: 0; }
            10% { opacity: 0.6; }
            90% { opacity: 0.6; }
            100% { transform: translate(100px, -800px); opacity: 0; }
        }

        .glow-orb {
            position: absolute;
            border-radius: 50%;
            filter: blur(80px);
            opacity: 0.4;
            animation: pulse-glow 6s ease-in-out infinite;
        }

        @keyframes pulse-glow {
            0%, 100% { transform: scale(1); opacity: 0.4; }
            50% { transform: scale(1.2); opacity: 0.6; }
        }

        .orb-1 {
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, #00f3ff 0%, transparent 70%);
            top: -100px;
            left: -100px;
        }

        .orb-2 {
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, #ff00e5 0%, transparent 70%);
            bottom: -150px;
            right: -150px;
            animation-delay: 3s;
        }

        .orb-3 {
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, #ffaa00 0%, transparent 70%);
            top: 50%;
            right: 10%;
            animation-delay: 1.5s;
        }

        /* Content */
        .content {
            position: relative;
            z-index: 1;
        }

        /* Header Section */
        .header {
            text-align: center;
            margin-bottom: 80px;
            position: relative;
        }

        .logo-container {
            display: inline-block;
            position: relative;
            margin-bottom: 30px;
        }

        .logo-ring {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 200px;
            height: 200px;
            border: 3px solid;
            border-color: #00f3ff transparent #ff00e5 transparent;
            border-radius: 50%;
            animation: rotate-ring 4s linear infinite;
        }

        @keyframes rotate-ring {
            0% { transform: translate(-50%, -50%) rotate(0deg); }
            100% { transform: translate(-50%, -50%) rotate(360deg); }
        }

        .logo-icon {
            font-size: 80px;
            position: relative;
            z-index: 1;
            filter: drop-shadow(0 0 20px rgba(0, 243, 255, 0.8));
        }

        .main-title {
            font-size: 6rem;
            font-weight: 900;
            background: linear-gradient(135deg, #00f3ff 0%, #ff00e5 50%, #ffaa00 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 20px;
            letter-spacing: 4px;
            text-transform: uppercase;
            animation: title-shimmer 3s ease-in-out infinite;
            position: relative;
            text-shadow: 0 0 60px rgba(0, 243, 255, 0.5);
        }

        @keyframes title-shimmer {
            0%, 100% { filter: brightness(1) saturate(1); }
            50% { filter: brightness(1.2) saturate(1.3); }
        }

        .subtitle {
            font-size: 2rem;
            color: #00f3ff;
            letter-spacing: 12px;
            text-transform: uppercase;
            font-weight: 300;
            margin-bottom: 20px;
        }

        .tagline {
            font-size: 1.3rem;
            color: #b0b0b0;
            max-width: 700px;
            margin: 0 auto;
            line-height: 1.8;
            font-weight: 300;
        }

        .badge-container {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 30px;
            flex-wrap: wrap;
        }

        .badge {
            padding: 10px 25px;
            background: rgba(0, 243, 255, 0.1);
            border: 2px solid #00f3ff;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: bold;
            color: #00f3ff;
            letter-spacing: 1px;
            transition: all 0.3s ease;
        }

        .badge:hover {
            background: rgba(0, 243, 255, 0.2);
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(0, 243, 255, 0.3);
        }

        .badge.pink {
            border-color: #ff00e5;
            color: #ff00e5;
            background: rgba(255, 0, 229, 0.1);
        }

        .badge.pink:hover {
            background: rgba(255, 0, 229, 0.2);
            box-shadow: 0 10px 30px rgba(255, 0, 229, 0.3);
        }

        .badge.gold {
            border-color: #ffaa00;
            color: #ffaa00;
            background: rgba(255, 170, 0, 0.1);
        }

        .badge.gold:hover {
            background: rgba(255, 170, 0, 0.2);
            box-shadow: 0 10px 30px rgba(255, 170, 0, 0.3);
        }

        /* BST Visualization */
        .bst-section {
            margin: 80px 0;
            position: relative;
        }

        .section-header {
            text-align: center;
            margin-bottom: 50px;
        }

        .section-title {
            font-size: 3rem;
            color: #fff;
            margin-bottom: 15px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .section-subtitle {
            color: #808080;
            font-size: 1.1rem;
        }

        .tree-display {
            background: rgba(255, 255, 255, 0.02);
            border: 2px solid rgba(0, 243, 255, 0.3);
            border-radius: 25px;
            padding: 60px 40px;
            backdrop-filter: blur(20px);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3),
                        inset 0 0 50px rgba(0, 243, 255, 0.05);
            position: relative;
            overflow: visible;
        }

        .tree-grid {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 40px;
        }

        .tree-level {
            display: flex;
            justify-content: center;
            gap: 30px;
            position: relative;
        }

        .tree-node {
            position: relative;
            padding: 20px 30px;
            background: linear-gradient(135deg, rgba(0, 243, 255, 0.15), rgba(255, 0, 229, 0.15));
            border: 3px solid;
            border-radius: 15px;
            font-size: 1.5rem;
            font-weight: bold;
            min-width: 80px;
            text-align: center;
            box-shadow: 0 0 30px rgba(0, 243, 255, 0.4);
            transition: all 0.3s ease;
            animation: node-pulse 3s ease-in-out infinite;
        }

        .tree-node:hover {
            transform: scale(1.1) translateY(-5px);
            box-shadow: 0 0 50px rgba(0, 243, 255, 0.8);
        }

        @keyframes node-pulse {
            0%, 100% { 
                box-shadow: 0 0 30px rgba(0, 243, 255, 0.4);
            }
            50% { 
                box-shadow: 0 0 50px rgba(0, 243, 255, 0.6);
            }
        }

        .node-root {
            border-color: #00f3ff;
            animation-delay: 0s;
        }

        .node-parent {
            border-color: #ff00e5;
            animation-delay: 0.5s;
        }

        .node-leaf {
            border-color: #ffaa00;
            font-size: 1.2rem;
            padding: 15px 25px;
            animation-delay: 1s;
        }

        .node-label {
            position: absolute;
            top: -30px;
            left: 50%;
            transform: translateX(-50%);
            font-size: 0.7rem;
            color: #808080;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        /* Connection Lines */
        .tree-line {
            position: absolute;
            height: 2px;
            background: linear-gradient(90deg, #ff00e5, #00f3ff);
            opacity: 0.4;
            transform-origin: left;
        }

        /* Stats Dashboard */
        .stats-dashboard {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            margin: 60px 0;
        }

        .stat-card {
            background: linear-gradient(135deg, rgba(0, 243, 255, 0.05), rgba(255, 0, 229, 0.05));
            border: 2px solid rgba(0, 243, 255, 0.3);
            border-radius: 20px;
            padding: 35px;
            text-align: center;
            backdrop-filter: blur(10px);
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 243, 255, 0.1), transparent);
            transition: left 0.6s;
        }

        .stat-card:hover::before {
            left: 100%;
        }

        .stat-card:hover {
            transform: translateY(-10px);
            border-color: #00f3ff;
            box-shadow: 0 15px 50px rgba(0, 243, 255, 0.4);
        }

        .stat-icon {
            font-size: 3rem;
            margin-bottom: 20px;
            filter: drop-shadow(0 0 10px rgba(0, 243, 255, 0.5));
        }

        .stat-value {
            font-size: 3.5rem;
            font-weight: 900;
            background: linear-gradient(135deg, #00f3ff, #ff00e5);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 10px;
        }

        .stat-label {
            color: #808080;
            font-size: 0.95rem;
            text-transform: uppercase;
            letter-spacing: 3px;
            font-weight: 600;
        }

        /* Features Grid */
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 30px;
            margin: 60px 0;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(0, 243, 255, 0.2);
            border-radius: 20px;
            padding: 40px;
            backdrop-filter: blur(15px);
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .feature-card::after {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(0, 243, 255, 0.1) 0%, transparent 70%);
            opacity: 0;
            transition: opacity 0.4s;
        }

        .feature-card:hover::after {
            opacity: 1;
        }

        .feature-card:hover {
            transform: translateY(-15px) scale(1.02);
            border-color: #00f3ff;
            box-shadow: 0 20px 60px rgba(0, 243, 255, 0.3);
        }

        .feature-icon {
            font-size: 3.5rem;
            margin-bottom: 25px;
            display: block;
            filter: drop-shadow(0 0 15px rgba(0, 243, 255, 0.6));
        }

        .feature-title {
            font-size: 1.6rem;
            color: #00f3ff;
            margin-bottom: 15px;
            font-weight: 700;
        }

        .feature-desc {
            color: #b0b0b0;
            line-height: 1.8;
            font-size: 1.05rem;
        }

        /* Commands Section */
        .commands-section {
            background: rgba(255, 255, 255, 0.02);
            border: 2px solid rgba(0, 243, 255, 0.2);
            border-radius: 25px;
            padding: 60px;
            margin: 80px 0;
            backdrop-filter: blur(20px);
        }

        .commands-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-top: 40px;
        }

        .command-card {
            background: rgba(0, 0, 0, 0.4);
            padding: 25px;
            border-radius: 15px;
            border-left: 4px solid #ff00e5;
            transition: all 0.3s ease;
            position: relative;
        }

        .command-card::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            width: 4px;
            height: 0;
            background: linear-gradient(180deg, #00f3ff, #ff00e5);
            transition: height 0.3s ease;
        }

        .command-card:hover::before {
            height: 100%;
        }

        .command-card:hover {
            background: rgba(0, 243, 255, 0.05);
            transform: translateX(15px);
            border-left-color: #00f3ff;
        }

        .command-name {
            color: #ffaa00;
            font-weight: 900;
            font-size: 1.3rem;
            margin-bottom: 10px;
            font-family: 'Courier New', monospace;
        }

        .command-syntax {
            color: #00f3ff;
            font-size: 0.9rem;
            font-family: 'Courier New', monospace;
            margin-bottom: 10px;
            opacity: 0.8;
        }

        .command-desc {
            color: #909090;
            font-size: 0.95rem;
            line-height: 1.6;
        }

        /* Tech Stack */
        .tech-section {
            text-align: center;
            margin: 80px 0;
        }

        .tech-grid {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 40px;
        }

        .tech-badge {
            padding: 15px 35px;
            background: rgba(255, 170, 0, 0.1);
            border: 2px solid #ffaa00;
            border-radius: 30px;
            color: #ffaa00;
            font-weight: 700;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            cursor: default;
        }

        .tech-badge:hover {
            background: rgba(255, 170, 0, 0.2);
            transform: scale(1.15) rotate(2deg);
            box-shadow: 0 10px 40px rgba(255, 170, 0, 0.4);
        }

        /* Complexity Table */
        .complexity-table {
            margin: 60px 0;
            background: rgba(255, 255, 255, 0.02);
            border-radius: 20px;
            padding: 40px;
            border: 1px solid rgba(0, 243, 255, 0.2);
        }

        .table-wrapper {
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0 10px;
        }

        thead {
            background: linear-gradient(135deg, rgba(0, 243, 255, 0.2), rgba(255, 0, 229, 0.2));
        }

        th {
            padding: 20px;
            text-align: left;
            color: #00f3ff;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-size: 0.95rem;
        }

        tbody tr {
            background: rgba(255, 255, 255, 0.03);
            transition: all 0.3s ease;
        }

        tbody tr:hover {
            background: rgba(0, 243, 255, 0.1);
            transform: scale(1.02);
        }

        td {
            padding: 20px;
            color: #b0b0b0;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
        }

        td:first-child {
            color: #ffaa00;
            font-weight: 700;
            font-family: 'Courier New', monospace;
        }

        .complexity-best {
            color: #00f3ff;
            font-weight: 700;
        }

        .complexity-worst {
            color: #ff00e5;
            font-weight: 700;
        }

        /* Footer */
        .footer {
            text-align: center;
            margin-top: 100px;
            padding: 50px;
            border-top: 2px solid rgba(0, 243, 255, 0.2);
            background: rgba(255, 255, 255, 0.02);
            border-radius: 20px;
        }

        .footer-title {
            font-size: 2rem;
            color: #00f3ff;
            margin-bottom: 20px;
            font-weight: 700;
        }

        .footer-text {
            color: #808080;
            font-size: 1.05rem;
            line-height: 2;
            max-width: 800px;
            margin: 0 auto;
        }

        .code-snippet {
            background: rgba(0, 0, 0, 0.5);
            padding: 20px 30px;
            border-radius: 10px;
            border-left: 4px solid #00f3ff;
            margin: 20px auto;
            max-width: 600px;
            text-align: left;
            font-family: 'Courier New', monospace;
            color: #00f3ff;
            font-size: 1rem;
        }

        @media (max-width: 768px) {
            .main-title {
                font-size: 3.5rem;
            }
            .subtitle {
                font-size: 1.5rem;
                letter-spacing: 6px;
            }
            .section-title {
                font-size: 2rem;
            }
            .tree-display {
                padding: 40px 20px;
            }
            .commands-section {
                padding: 40px 30px;
            }
        }
    </style>
</head>
<body>
    <div class="bg-animation">
        <div class="glow-orb orb-1"></div>
        <div class="glow-orb orb-2"></div>
        <div class="glow-orb orb-3"></div>
    </div>

    <div class="container">
        <div class="content">
            <!-- Header -->
            <header class="header">
                <div class="logo-container">
                    <div class="logo-ring"></div>
                    <div class="logo-icon">⚔️</div>
                </div>
                <h1 class="main-title">Arena of Ratings</h1>
                <p class="subtitle">Binary Search Tree</p>
                <p class="tagline">
                    Elite matchmaking & leaderboard system powered by advanced BST architecture.<br>
                    Developed for Data Structures Lab Final Exam
                </p>
                <div class="badge-container">
                    <span class="badge">C++ Implementation</span>
                    <span class="badge pink">Pointer-Based</span>
                    <span class="badge gold">No STL Containers</span>
                    <span class="badge">O(log n) Average</span>
                </div>
            </header>

            <!-- Stats Dashboard -->
            <div class="stats-dashboard">
                <div class="stat-card">
                    <div class="stat-icon">⚡</div>
                    <div class="stat-value">O(log n)</div>
                    <div class="stat-label">Average Complexity</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">🎯</div>
                    <div class="stat-value">15+</div>
                    <div class="stat-label">Commands</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">🌳</div>
                    <div class="stat-value">100%</div>
                    <div class="stat-label">Pointer-Based</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">🚫</div>
                    <div class="stat-value">0</div>
                    <div class="stat-label">STL Containers</div>
                </div>
            </div>

            <!-- BST Visualization -->
            <section class="bst-section">
                <div class="section-header">
                    <h2 class="section-title">Binary Search Tree Structure</h2>
                    <p class="section-subtitle">Self-balancing player hierarchy based on rating</p>
                </div>
                <div class="tree-display">
                    <div class="tree-grid">
                        <div class="tree-level">
                            <div class="tree-node node-root">
                                <span class="node-label">Root</span>
                                50
                            </div>
                        </div>
                        <div class="tree-level">
                            <div class="tree-node node-parent">
                                <span class="node-label">Parent</span>
                                30
                            </div>
                            <div class="tree-node node-parent">
                                <span class="node-label">Parent</span>
                                70
                            </div>
                        </div>
                        <div class="tree-level">
                            <div class="tree-node node-leaf">
                                <span class="node-label">Leaf</span>
                                20
                            </div>
                            <div class="tree-node node-leaf">
                                <span class="node-label">Leaf</span>
                                40
                            </div>
                            <div class="tree-node node-leaf">
                                <span class="node-label">Leaf</span>
                                60
                            </div>
                            <div class="tree-node node-leaf">
                                <span class="node-label">Leaf</span>
                                80
                            </div>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Features -->
            <section>
                <div class="section-header">
                    <h2 class="section-title">Core Features</h2>
                    <p class="section-subtitle">Advanced BST operations for competitive gaming</p>
                </div>
                <div class="features-grid">
                    <div class="feature-card">
                        <span class="feature-icon">⚔️</span>
                        <h3 class="feature-title">Dynamic Management</h3>
                        <p class="feature-desc">Efficient player join/leave operations with automatic tree restructuring and memory management</p>
                    </div>
                    <div class="feature-card">
                        <span class="feature-icon">🎯</span>
                        <h3 class="feature-title">Smart Matchmaking</h3>
                        <p class="feature-desc">Find closest-rated opponents using successor/predecessor queries in logarithmic time</p>
                    </div>
                    <div class="feature-card">
                        <span class="feature-icon">📊</span>
                        <h3 class="feature-title">Advanced Ranking</h3>
                        <p class="feature-desc">Real-time ranking system with K-th player lookup and range queries</p>
                    </div>
                    <div class="feature-card">
                        <span class="feature-icon">💚</span>
                        <h3 class="feature-title">Health System</h3>
                        <p class="feature-desc">Dynamic damage and healing mechanics with bounds checking and state management</p>
                    </div>
                    <div class="feature-card">
                        <span class="feature-icon">🌳</span>
                        <h3 class="feature-title">Tree Analytics</h3>
                        <p class="feature-desc">Comprehensive statistics including height, leaf count, and structural analysis</p>
                    </div>
                    <div class="feature-card">
                        <span class="feature-icon">🔗</span>
                        <h3 class="feature-title">Distance Calculation</h3>
                        <p class="feature-desc">Compute BST path distance using Lowest Common Ancestor algorithm</p>
                    </div>
                </div>
            </section>

            <!-- Commands -->
            <section class="commands-section">
                <div class="section-header">
                    <h2 class="section-title">Command Arsenal</h2>
                    <p class="section-subtitle">Complete set of BST operations</p>
                </div>
                <div class="commands-grid">
                    <div class="command-card">
                        <div class="command-name">JOIN</div>
                        <div class="command-syntax">JOIN rating name hp</div>
                        <div class="command-desc">Insert new player into the arena</div>
                    </div>
                    <div class="command-card">
                        <div class="command-name">LEAVE</div>
                        <div class="command-syntax">LEAVE rating</div>
                        <div class="command-desc">Remove player from arena</div>
                    </div>
                    <div class="command-card">
                        <div class="command-name">STATUS</div>
                        <div class="command-syntax">STATUS rating</div>
                        <div class="command-desc">Display player information</div>
                    </div>
                    <div class="command-card">
                        <div class="command-name">DAMAGE</div>
                        <div class="command-syntax">DAMAGE rating amount</div>
                        <div class="command-desc">Decrease player health points</div>
                    </div>
                    <div class="command-card">
                        <div class="command-name">HEAL</div>
                        <div class="command-syntax">HEAL rating amount</div>
                        <div class="command-desc">Increase player health points</div>
                    </div>
                    <div class="command-card">
                        <div class="command-name">NEXT</div>
                        <div class="command-syntax">NEXT X</div>
                        <div class="command-desc">Find successor (next higher rating)</div>
                    </div>
                    <div class="command-card">
                        <div class="command-name">PREV</div>
                        <div class="command-syntax">PREV X</div>
                        <div class="command-desc">Find predecessor (next lower rating)</div>
                    </div>
                    <div class="command-card">
                        <div class="command-name">MATCH</div>
                        <div class="command-syntax">MATCH X</div>
                        <div class="command-desc">Find player with closest rating to X</div>
