#include <iostream>
using namespace std;

int GenerateCode(int n)
	{
		int Factorial = 1;
		for(int i=n; i>0; i--)
		{
			Factorial *= n;
			n--; 
		}
		return Factorial;
	}
int(*ptrC)(int n) = &GenerateCode;

int main() {
	
	int a;
	cout << "Enter Number for to find Factorial: ";
	cin >> a;
	int z = ptrC(a);
	cout << "The Factorial is: " << z;
	
	return 0;
}
